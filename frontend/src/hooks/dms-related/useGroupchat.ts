import { useQueryClient } from "@tanstack/react-query";

interface GroupChatTools {

}

export default function useGroupChat():GroupChatTools {
    const queryClient = useQueryClient();


    return {  }
}